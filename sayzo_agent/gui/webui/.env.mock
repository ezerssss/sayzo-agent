# Loaded by `vite --mode mock` (see `npm run dev:mock`).
# Switches `settings-bridge.ts` over to in-memory mock data so the Captures
# pane (and any other future panes) can be designed and previewed without
# the Python agent running.
VITE_USE_MOCK_BRIDGE=1
